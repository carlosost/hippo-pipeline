# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/

# --- Environments / toolchain ---
.venv/
venv/
.python-version
.uv/

# --- Test & type caches ---
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# --- Secrets ---
# The pipeline reads files and writes files; it holds no credentials (docs/OPERATIONS.md 8).
# These lines are a guard for the day that changes, not a promise that a .env.example exists.
.env
.env.*

# --- Pipeline outputs & quarantine (regenerated, never reviewed as source) ---
out/
output/
.quarantine/

# --- OS / editor noise ---
.DS_Store
.idea/
.vscode/
*.swp
