# Gherkin acceptance scenarios

One `.feature` file per feature in `memory/features/`. Written **before** the
implementation (GENERAL_ENGINEERING_PLAYBOOK.md 1.6) and wired to step definitions in
`tests/bdd/` via pytest-bdd.

Empty on purpose: no feature has been specified yet. The first `.feature` file lands
in the same commit as `memory/features/feature-01-*.md`.
